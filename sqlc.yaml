version: "2"
sql:
  - engine: "postgresql"
    queries: "/queries"
    schema: "schema.sql"
    gen:
      go:
        package: "store"
        out: "store"
        emit_json_tags: true
        sql_package: "lib/pq"